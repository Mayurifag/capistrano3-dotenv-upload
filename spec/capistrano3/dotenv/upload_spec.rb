RSpec.describe Capistrano3::Dotenv::Upload do
  it "has a version number" do
    expect(Capistrano3::Dotenv::Upload::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
